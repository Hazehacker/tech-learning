# 单元测试

![image-20250923134736289](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20250923134736289.png)

![image-20250923135115124](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20250923135115124.png)

![image-20250923134812906](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20250923134812906.png)

* ```
  public class StringUtilTest{
  	//测试方法：必须是公开public，无参、无返回值
  	//测试方法必须加上@Test注解（菌体框架的核心步骤）
  	@Test
  	public void testPrintNumber(){
  		//测试步骤
  		//1. 创建业务类对象
  		StringUtil su = new StringUtil();
  		
  		//2. 调用业务方法，写入全面的测试用例
  		su.printNumber("张三");
  		su.printNumber("");
  		su.printNumber("NULL");
  		
  		//3. 断言（如果有必要）
  		
  	}
  	
  	@Test
  	public void testGetMaxIndex(){
  		int index = StringUtil.getMaxIndex("abcdefg");
  		int index2 = StringUtil.getMaxIndex("");
  		int index3 = StringUtil.getMaxIndex("null");
  		
  		System.out.println(index);
  		System.out.println(index2);
  		System.out.println(index3);
  		
  		Assert.assertEquals("本轮测试失败，业务获取的最大索引有问题，请检查",6,index);
  		Assert.assertEquals("本轮测试失败，业务获取的最大索引有问题，请检查",6,index);
  		Assert.assertEquals(,"本轮测试失败，业务获取的最大索引有问题，请检查",6,index);
  		
  	}
  	
  }
  
  
  
  ```

  * **做断言**
    **断言结果与预期是否一致**

  * 右键方法，"Run xxx()"

    或右键整个类

    在下方查看是否通过（红或绿）





























































# 反射



* 反射的作用

  ![image-20250923162513470](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20250923162513470.png)基本上所有框架都会用反射来设计一些功能





* 认识反射

  ![image-20250923142013074](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20250923142013074.png)

  ![](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20250923142138307.png)

  > #### 反射的操作逻辑和原本比起来，是关系反转的

* 一：获取class对象

  ![image-20250923142302486](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20250923142302486.png)

  ```
  public class Student{
  	private String name;
  	private Integer id;
  	
  }
  ```

  ```
  public class temp{
  	public static void main(String[] args){
  		class c1 = Student.class;
  		System.out.println(c1);
  		
  		class c2 = Class.forName("com.itheima.demo2.Student")
  		System.out.println(c2);
  		
  		
  		
  	}
  }
  ```

  

* 获取类中的成分及操作

  ![image-20250923143240221](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20250923143240221.png)
   ![image-20250923143311608](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20250923143311608.png)

  ![image-20250923143353793](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20250923143353793.png)

  

  ```
  Class c1 = Student.class;
  System.out.println(c1.getName());//获取全类名 com.itheima.demo.Student
  System.out.println(c1.getSimpleName());//类名 Student
  
  //【获取类的构造器对象】
  //获取所有构造器
  Constructor[] cons = c1.getDeclaredConstructors();//拿到所有的构造器对象（包括私有）
  for(Constructor con: cons){
  	System.out.println(con.getName() + "(" + con.getParameterCount() + ")");
  }
  
  //获取单个构造器
  Constructor con1 = c1.getDeclaredConstructor();//无参构造器
  System.out.println(con1.getName() + "(" + con1.getParameterCount() + ")");
  
  //匹配，定位特定的构造器（根据类型定位）
  Constructor con2 = c1.getDeclaredConstructor(String.class,int.class);//代表第一个参数类型是String，第二个参数类型是int类型
  
  //《获取的构造器可以用来构造对象》
  Student student = (Student) con1.newInstance();
  
  
  
  
  //【获取类的成员变量】
  
  //获取所有成员变量
  Field[] fields = c1.getDeclaredFields();
  for(Field field: fields){
  	System.out.println(field.getName() + "(" + field.getType().getName() + ")");
  }
  //获取单个成员变量(根据名字定位)
  Field field = c1.getDeclaredField("hobby");
  System.out.println(field.getName() + "(" + field.getType().getName() + ")");
  
  //《获取成员变量可以用来取值和赋值》
  field.setAccessible(true);
  field.set(c1,"社交");
  System.out.println(c1);
  String hobby = (String)field.get(c1);
  
  
  
  
  
  //【获取成员方法】
  //获取所有方法
  Method[] methods = c1.getDeclaredMethods();
  for(Method method :methods){
  	System.out.println(method.getName() + "(" + method.getParameterCount() + ")");
  }
  
  //获取单个成员方法对象
  Method method1 = c1.getDeclaredMethod("learning");//获取无参数的learning方法
  Method method2 = c1.getDeclaredMethod("learning",String.class);//获取有参数的learning方法
  
  //《获取成员方法可以用来调用方法》
  method1.setAccessible(true);//对于private的:绕过访问权限，直接访问
  method1.invoke(c1);//唤醒对象c1的method1执行，相当于c1对象调用method1
  
  //
  Object res = method2.invoke(c1,"牛肉");//有参、有返回值的方法
  
  
  
   
  ```

  

* 





































































# 注解



> 重点内容，一个项目几百个注解

* 作用

  * ![image-20250924205102099](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20250924205102099.png)

    > 正如test方法有了注解才能跑







### 自定义注解





* 示例

  ```
  public @interface MyBook{
  	String name();
  	int age() default 18;
  	String[] address();
  }
  
  
  
  
  
  
  ```

  ![image-20250924205734820](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20250924205734820.png)

  > 属性书写：
  > 前面public和private不用写，属性名后面要加括号

* 如果只有一个属性，就可以省略属性名  xx = "y"，直接写"y"



*  ![image-20250924210142169](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20250924210142169.png)



### 元注解

> 用于修饰注解
>
> 注解之上的注解

![image-20250924210337060](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20250924210337060.png)

![image-20250924210606395](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20250924210606395.png)

![image-20250924210927031](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20250924210927031.png)

> Retention一般用的是RunTime







* 注解的解析
  ![image-20250924211057255](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20250924211057255.png)



















# 动态代理



## 是什么



> 动态代理技术广泛用于AOP、
>
> 











# 流方式代码

> * 用流的方式代替遍历，一行代码完成操作
>
> 
>
> ```java
> List<Employee> employeeList = employeeService.selectAll(null);
>         //拿出部门名称
>         //（虽然employee表里面没有department属性，但是selectAll采用关联查询，也查询到了departmentName并存在对象中、于是后面employee就能get到departmentName）
> 
> Set<String> departmentNameSet = employeeList.stream().map(Employee::getDepartmentName).collect(Collectors.toSet());
> ```
>
> > 首先，通过`stream()`方法将列表转换为流；然后，使用`map(Employee::getDepartmentName)`对流中的每个元素调用`getDepartmentName()`方法，提取出部门名称，形成一个新的流；最后，通过`collect(Collectors.toSet())`将流中的元素收集到一个集合中，并确保集合中的元素是唯一的，即去重。最终，`departmentNameSet`集合中存储了所有员工所属部门的唯一名称。
> >
> >
> > 它接受一个函数作为参数（在 Java 中，这个函数通常是用方法引用或 Lambda 表达式表示的），然后对流中的每个元素应用这个函数，并生成一个新的流,它不仅限于提取属性，还可以用来对元素进行各种操作，比如类型转换、格式化、甚至是数学运算。`map()` 和 `collect()` 是紧密配合的，前者负责数据的“加工”，后者负责数据的“收集”
>
> 

* 有了orderCountList和validOrderCountList，获取订单总数和有效订单总数

  ```
  Integer totalOrderCount = orderCountList.stream().reduce(Integer::sum).get();
  Integer validOrderCount = validOrderCOuntList.stream().reduce(Integer::sum).get();
  ```

  

* 从一个对象集合中通过流获得另一个集合

  for写法

  ![image-20251009160401647](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20251009160401647.png)

  stream写法

  ![image-20251009160519360](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20251009160519360.png)



* 将一个对象数据转换成 长字符串

  ![image-20251010093740443](C:\Users\毕哲晖\AppData\Roaming\Typora\typora-user-images\image-20251010093740443.png)

  ```
  List<OrderDetail> orderDetailList = orderDetailMapper.getByOrderId(orders.getId());
  
  List<String> orderDishList = orderDetailList.stream().map(x -> {
      String orderDish = x.getName() + "*" + x.getNumber() + ";";
      return orderDish;
  }).collect(Collectors.toList());
  // 将该订单对应的所有菜品信息拼接在一起
  String orderDishes = String.join("", orderDishList);
  ```

* 



